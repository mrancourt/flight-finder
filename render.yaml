# Render Blueprint for Flight Finder
# Using Docker for FREE tier deployment
services:
  # Backend API Service (Docker - FREE)
#  - type: web
#    name: flights-api
#    env: docker
#    region: oregon
#    branch: main
#    dockerfilePath: ./Dockerfile
#    envVars:
#      - key: FLIGHTS_CSV
#        value: /app/flights.csv
#    healthCheckPath: /api/flights

  # Frontend Static Site (FREE)
  - type: web
    name: flights-ui
    env: static
    branch: main
    buildCommand: "cd client && npm install && npm run build"
    staticPublishPath: ./client/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
